"use client";
import React from "react";

// Footer is now integrated into Hero for the minimal layout
export default function Footer({ ...props }: React.HTMLProps<HTMLDivElement>) {
  return null;
}
